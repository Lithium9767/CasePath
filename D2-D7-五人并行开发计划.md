# CasePath D2—D7 五人并行开发计划

> 基线：D1 契约优先骨架已经完成并通过 8 项测试  
> 目标：一周结束时交付一条真实来源、可追问、可更新、可解释的纵向链路  
> 演示域：服务合同履行地点变化、履行不能、解除与返还  
> 主案例：曾某诉武汉某健身管理有限公司服务合同纠纷案

---

## 1. 本周唯一完成目标

```text
用户输入民事问题
  → 候选请求权
  → 检索真实法条与规则条件
  → 检索支持/限制/边界案例
  → 投影为规则条件矩阵
  → 选择一个高价值追问
  → 接收用户回答并更新状态
  → 生成带法条、案例和原文跨度的ExplanationPlan
  → 前端展示最终解释及不确定性
```

本周不以“代码文件数量”“图节点数量”作为成功标准。只有上述链路使用真实、可回放数据连续运行，才算完成。

---

## 2. 开工前的合同修正

### 2.1 不得把1,260条法条直接视为1,260条规则

需要区分：

```text
LegalSourceRecord：整部法律及版本
ProvisionRecord：一条具体法条原文
RuleRecord：从一个或多个法条、解释、案例中整理出的适用规则
```

关系为：

```text
LegalSourceRecord
  └─ CONTAINS → ProvisionRecord
                    └─ EXPRESSES → RuleRecord
                                         ├─ REQUIRES → RuleCondition
                                         ├─ HAS_EXCEPTION → RuleException
                                         └─ LEADS_TO → LegalConsequence
```

因此P2的准确交付应为：

- 导入1个《中华人民共和国民法典》`LegalSourceRecord`；
- 导入1,260个`ProvisionRecord`；
- 基于第509、563、566条整理3—5个`RuleRecord`；
- 只有经过来源核验和人工校正的规则才能标记为L3。

### 2.2 v1.1合同冻结点

D2上午只允许新增：

```text
LegalSourceRecord
ProvisionRecord
ErrorResponse
AnswerRequest
CapabilityStatus
```

原五个合同继续保留：

```text
RuleRecord
CaseRecord
QueryState
RetrievalBundle
ExplanationPlan
```

D2中午12:00后，任何公共合同修改必须由P1批准，并同时完成：

1. Pydantic模型；
2. JSON Schema；
3. examples示例；
4. contract_version变更说明；
5. 全部契约测试。

---

## 3. 文件所有权

| 人员 | 独占目录 | 可提交内容 | 不得直接修改 |
|---|---|---|---|
| P1 | `api/`、`workflow/`、`config/`、`repositories/` | API、状态机、配置、依赖注入、存储接口 | P2/P3的数据内容、P4评分公式、P5页面 |
| P2 | `ingestion/laws/`、`rule_layer/`、`data/canonical/rules/` | 法源、条文、规则、条件、版本、法条跨度 | `workflow/`、案例模型、前端 |
| P3 | `ingestion/cases/`、`case_layer/`、`data/canonical/cases/` | 案例清洗、请求权、法院认定、裁判结果、案例跨度 | 规则条件定义、API、前端 |
| P4 | `retrieval/`、`projection/`、`clarification/` | BM25、结构重排、条件投影、案例对比、追问评分 | 原始数据、API协议、前端样式 |
| P5 | `frontend/`、`tests/e2e/`、`data/eval/` | Streamlit页面、交互、测试样例、演示脚本 | 公共合同和后端业务逻辑 |

以下文件只能由P1合并：

```text
pyproject.toml
uv.lock
src/casepath/contracts/**
src/casepath/ports/**
src/casepath/bootstrap.py
```

其他成员需要改公共合同，应提交“合同变更请求”，不能直接在自己分支增加近义字段。

### 3.1 总体架构由谁负责

P1不是普通API开发人员，而是本周的系统架构负责人。P1负责先搭建完整系统骨架，P2—P5在固定位置实现模块。

```text
┌────────────────────────────────┐
│ 5. 前端交互层                  │ P5
├────────────────────────────────┤
│ 4. API、会话与工作流层         │ P1
├────────────────────────────────┤
│ 3. 检索、条件投影与追问算法层  │ P4
├────────────────────────────────┤
│ 2. 两层法律知识图谱            │
│    规范规则层                  │ P2
│    案例适用层                  │ P3
│    公共图存储接口与约束        │ P1
├────────────────────────────────┤
│ 1. 数据与基础设施层            │
│    公共配置、Repository、LLM   │ P1
│    法条数据                    │ P2
│    案例数据                    │ P3
└────────────────────────────────┘
```

各角色的准确边界：

| 人员 | 负责回答的问题 |
|---|---|
| P1 | 系统先调用谁、后调用谁，模块如何装配，失败如何降级 |
| P2 | 法律规定了什么规则，规则需要检查哪些条件 |
| P3 | 历史案例认定了什么事实，这些事实满足哪些规则条件 |
| P4 | 用户问题应检索什么，案例如何比较，哪个未知条件最值得追问 |
| P5 | 用户如何输入、回答和查看规则、案例、条件与最终解释 |

P1负责技术上的整体串联；P4负责法律计算上的规则—案例—用户串联。P4不得另建一套主工作流，P1也不得在API中重新实现P4的评分逻辑。

### 3.2 两层知识图谱如何连接

规则层与案例层的连接点不是“共同出现相同法条”，而是P2定义的`RuleCondition`。

P2输出规则条件：

```json
{
  "condition_id": "cond.performance.impossible",
  "label": "经营者无法继续履行",
  "predicate": "经营者是否已经无法继续提供约定服务"
}
```

P3从裁判原文中提取法院认定，并引用同一个`condition_id`：

```json
{
  "case_id": "case.zeng-v-wuhan-fitness.2021",
  "condition_id": "cond.performance.impossible",
  "status": "SATISFIED",
  "finding_ids": ["finding.performance-place-changed"],
  "source_span_ids": ["span.case.zeng.reasoning.01"]
}
```

图中形成：

```text
规范规则层
LegalSource
  → Provision
  → LegalRule
  → RuleCondition
          ↑
          │ SATISFIES / NOT_SATISFY
          │
    CourtFinding
          ↑
    ReasoningStep
          ↑
       Claim
          ↑
        Case
案例适用层
```

主要桥接边：

```text
Claim          ─INVOKES──────→ LegalRule
CourtFinding   ─SATISFIES────→ RuleCondition
CourtFinding   ─NOT_SATISFY──→ RuleCondition
ReasoningStep  ─APPLIES──────→ LegalRule
DecisionItem   ─REALIZES─────→ LegalConsequence
```

责任归属：

- P2定义`RuleCondition`及其法律依据；
- P3使用P2已经发布的`condition_id`建立历史案例桥接；
- P4将运行时用户事实映射到同一批`condition_id`；
- P1负责Neo4j约束、Repository和调用顺序；
- P5只展示桥接结果，不自行判断条件状态。

如果P3发现案例事实无法对应现有条件，只能提交“候选条件建议”给P2审核，不能直接创建近义条件。否则会出现：

```text
P2: cond.performance.impossible
P3: cond.cannot.continue.service
```

两者含义相同但ID不同，导致两层无法计算连接。

### 3.3 历史案例桥接与用户投影的区别

```text
历史案例事实 → 规则条件：P3离线构建，必须有裁判原文跨度
用户当前事实 → 规则条件：P4在线计算，必须保留用户原话或回答
```

例如：

```text
P3：案例A法院认定“全部门店停止经营”
    → cond.performance.impossible = SATISFIED

P4：用户只说“店关门了”
    → cond.performance.impossible = UNKNOWN
```

系统不能因为历史案例满足某条件，就把用户案件也自动标记为满足。

### 3.4 CasePath核心算法如何使用两层知识图谱

CasePath不是第三张图，而是读取两层图谱并进行检索、投影、比较和追问的算法。

输入：

```text
用户问题QueryState
+ P2提供的规则与条件
+ P3提供的案例与桥接关系
```

输出：

```text
RetrievalBundle
+ QueryConditionState
+ QuestionCandidate
+ ExplanationPlan
```

核心计算分四步：

#### A. Rule Retrieval

```text
用户问题
  → 候选请求权
  → 检索LegalRule
  → 取得RuleCondition集合
```

#### B. Rule-conditioned Case Retrieval

```text
候选规则与已知条件
  → 检索同一规则下的案例
  → 形成SupportSet、LimitSet、BoundarySet
```

案例的裁判结果不得进入第一阶段召回文本，只能在召回后的对比分组中使用。

#### C. Cross-layer Projection

将用户和候选案例投影到同一条件矩阵：

| 规则条件 | 用户 | 支持案例A | 限制案例B |
|---|---|---|---|
| 合同成立 | 满足 | 满足 | 满足 |
| 已付款 | 满足 | 满足 | 满足 |
| 有未履行余额 | 满足 | 满足 | 满足 |
| 无法继续履行 | 未知 | 满足 | 不满足 |
| 无替代履行 | 未知 | 满足 | 不满足 |

#### D. High-value Clarification

从用户的`UNKNOWN`条件中选择最值得追问的一项：

\[
Utility(c)=规则重要性+案例分化+解释信息增益+用户可回答性-交互成本
\]

示例：

```text
cond.performance.impossible  0.88
cond.alternative.performance 0.76
cond.refund.term              0.51
```

系统优先询问：

> 健身房是永久停止经营，还是暂时关闭？

用户回答后，P4更新条件状态并重新运行检索和对比；P1负责保存会话并按照相同工作流再次调用；P5展示更新后的矩阵和最终解释。

### 3.5 P1与P4的代码关系

P1负责调用顺序：

```python
rules = rule_retriever.retrieve(query_state)
cases = case_retriever.retrieve(query_state, rules)
projected = condition_projector.project(query_state, cases)
question = question_policy.select(projected, cases)
plan = explanation_planner.build(projected, cases)
```

P4负责这些对象内部如何计算：

```text
规则如何排序
案例如何排序
事实如何映射到条件
正反案例如何分组
追问价值如何计算
什么时候停止追问
```

### 3.6 最小跨层贯通验收

D2中午前必须完成一条真实贯通样例：

```text
P2：定义cond.performance.impossible及法条原文跨度
  ↓
P3：用同一condition_id连接健身房案例的法院认定和原文跨度
  ↓
P4：把用户“店关门了”映射为该条件的UNKNOWN
  ↓
P4：根据支持/限制案例分化生成关键追问
  ↓
P1：通过WorkflowSnapshot和API返回
  ↓
P5：展示问题、条件状态、案例差异及原文
```

验收失败条件：

- P2和P3使用不同`condition_id`；
- 案例桥接没有`SourceSpan`；
- 用户未说明的事实被标记为`NOT_SATISFIED`；
- P4绕过规则条件只根据文本相似度追问；
- P1在API层重新计算法律条件；
- P5根据展示需要自行修改后端状态。

---

## 4. P1：API、错误模型、配置与集成

### 4.1 目标

建立稳定的会话API和基础设施端口，使P2—P5只需要实现合同，不需要理解彼此内部代码。

### 4.2 文件规划

```text
src/casepath/
├─ api/
│  ├─ main.py
│  ├─ routes/
│  │  ├─ sessions.py
│  │  ├─ answers.py
│  │  └─ contracts.py
│  └─ errors.py
├─ config/
│  ├─ settings.py
│  └─ capabilities.py
├─ repositories/
│  ├─ interfaces.py
│  ├─ memory_session.py
│  └─ neo4j_graph.py
└─ bootstrap.py
```

### 4.3 API定义

#### 创建并分析会话

```http
POST /v1/sessions
Content-Type: application/json

{
  "query": "我在健身房充了5000元，店关门了，还有3000元没消费。"
}
```

返回：

```json
{
  "session_id": "...",
  "query_state": {},
  "retrieval_bundle": {},
  "next_question": {},
  "explanation_plan": {}
}
```

#### 提交追问回答

```http
POST /v1/sessions/{session_id}/answers

{
  "question_id": "question.performance_impossible.1",
  "condition_id": "cond.performance_impossible",
  "answer": "全部门店已经永久停止经营",
  "selected_option": "永久停止经营"
}
```

后端负责将回答转换为条件状态，不允许前端直接提交`SATISFIED`作为法律判断。

#### 查询会话

```http
GET /v1/sessions/{session_id}
```

#### 查询系统能力

```http
GET /v1/capabilities
```

返回：

```json
{
  "neo4j": "available|degraded|unavailable",
  "rule_retrieval": "available|degraded|unavailable",
  "case_retrieval": "available|degraded|unavailable",
  "llm": "available|degraded|unavailable",
  "citation_verification": "available|degraded|unavailable"
}
```

### 4.4 统一错误模型

```json
{
  "error_code": "CASEPATH_RULE_RETRIEVAL_UNAVAILABLE",
  "message": "规则检索暂时不可用",
  "trace_id": "...",
  "recoverable": true,
  "details": {}
}
```

最少定义：

```text
CASEPATH_VALIDATION_ERROR
CASEPATH_SESSION_NOT_FOUND
CASEPATH_INVALID_ANSWER
CASEPATH_RULE_RETRIEVAL_UNAVAILABLE
CASEPATH_CASE_RETRIEVAL_UNAVAILABLE
CASEPATH_GRAPH_UNAVAILABLE
CASEPATH_CITATION_NOT_VERIFIED
CASEPATH_INTERNAL_ERROR
```

### 4.5 Neo4j端口

P1只定义和接通存储端口，不负责规则、案例语义：

```python
class LegalGraphRepository(Protocol):
    def health(self) -> CapabilityStatus: ...
    def upsert_legal_source(self, source: LegalSourceRecord) -> None: ...
    def upsert_provision(self, provision: ProvisionRecord) -> None: ...
    def upsert_rule(self, rule: RuleRecord) -> None: ...
    def upsert_case(self, case: CaseRecord) -> None: ...
    def find_rules(self, rule_ids: list[str]) -> list[RuleRecord]: ...
    def find_cases(self, case_ids: list[str]) -> list[CaseRecord]: ...
```

实际Neo4j不可用时使用内存Repository，API仍可启动并返回降级标志。

### 4.6 P1完成标准

- 创建会话、提交回答、查询会话三个接口可运行；
- 404、422、503均返回统一错误结构；
- 会话至少可在内存中保持到进程结束；
- Neo4j不可用不会导致整个API启动失败；
- `/v1/capabilities`准确反映当前能力；
- API测试覆盖成功、无效输入、会话不存在和组件降级。

---

## 5. P2：法条导入与规范规则层

### 5.1 输入

数据来源仓库：`https://github.com/litunan/legal-rag`（`main` 分支）。以下路径以 `CasePath/` 仓库根目录为基准，并假定 `CasePath/` 与 `legal-rag/` 位于同一父目录：

```text
../legal-rag/data/laws/民法典_法条.json
../legal-rag/data/laws/民法典_统计.json
国家法律法规数据库核验信息
```

### 5.2 目标输出

```text
data/canonical/rules/legal_sources.jsonl      1条
data/canonical/rules/provisions.jsonl         1,260条
data/canonical/rules/rules.jsonl              3—5条L3演示规则
data/canonical/rules/source_spans.jsonl       对应法条原文跨度
data/manifests/civil_code.manifest.json       来源、日期、版本、哈希
```

### 5.3 ID规范

```text
source_id:
  law.prc.civil_code.2021

provision_id:
  law.prc.civil_code.2021.article_0509
  law.prc.civil_code.2021.article_0563
  law.prc.civil_code.2021.article_0566

rule_id:
  rule.contract.performance.good_faith.v1
  rule.contract.termination.nonperformance.v1
  rule.contract.termination.restitution.v1

condition_id:
  cond.contract.exists
  cond.obligation.unperformed
  cond.performance.impossible
  cond.alternative.performance
```

编号统一采用四位阿拉伯数字，禁止同时生成“第五百六十三条”和“563条”两个节点。

### 5.4 第509、563、566条处理方式

不是把每个法条机械生成一条规则，而是先确定演示请求权：

```text
Claim：服务合同解除并返还未消费费用
```

再整理候选规则：

```text
R1 合同履行应遵循全面履行和诚信原则
R2 符合法定情形时当事人可以解除合同
R3 合同解除后，未履行部分终止履行
R4 已履行部分根据履行情况和合同性质处理恢复原状、补救或赔偿
```

每条规则需要：

- 明确来源条文；
- 成立条件；
- 例外或限制；
- 法律后果；
- 用户可回答的事实问题；
- 可以支持该条件的证据类型；
- 原文跨度；
- 人工复核标记。

P2负责规则语义，P4只读取条件，不得自行创造新法律条件。

### 5.5 数据检查

```text
总条数 == 1260
article_0001 ... article_1260 唯一
不存在空原文
不存在重复provision_id
509、563、566条均能按ID精确读取
规则引用的provision_id全部存在
规则条件引用的source_span全部存在
```

### 5.6 P2完成标准

- 1,260条法条100%通过Schema；
- 条号唯一性100%；
- 509、563、566条原文和版本经过权威来源核验；
- 至少3个`RuleRecord`达到L3；
- 每个L3条件都有原文依据或明确标注为案例归纳；
- P4可以只依赖`rules.jsonl`建立规则检索索引。

---

## 6. P3：45件民事案例清洗与健身房案例深加工

### 6.1 输入

数据来源及路径基准同5.1：

```text
../legal-rag/data/processed/processed_cases.json
../legal-rag/data/processed/markdown_docs/*.md
```

原数据51件，其中民事45件；必须按`case_category == 民事`过滤，刑事、行政和执行案件不得进入默认民事索引。

### 6.2 两级输出

#### 45件轻量案例L1

```text
case_id
title
case_no
case_category
case_cause
court
judgment_date
keywords
case_facts_text
judgment_reasoning_text
judgment_summary
related_provision_ids
source_file
```

用途：BM25广召回，不用于深度条件判断。

#### 8—12件深度案例L2/L3

至少包含：

```text
ClaimRecord
CourtFinding
ConditionFinding
ReasoningStep
DecisionItem
SourceSpan
```

### 6.3 健身房案例优先修复

目标案例：

```text
曾某诉武汉某健身管理有限公司服务合同纠纷案
（2021）鄂0104民初109号
```

已知问题：

- 原数据`judgment_result`为空；
- 裁判信息可能混在基本案情或全文中；
- 当前只有摘要级信息，不足以直接构造完整裁判主文；
- 必须从原始文书或可靠公开来源补齐，不能由LLM猜测。

处理顺序：

1. 固定原始文档并计算SHA-256；
2. 按标题切分裁判要旨、基本案情、裁判理由、裁判结果；
3. 建立段落ID和字符偏移；
4. 识别请求权；
5. 抽取法院认定事实；
6. 对齐P2定义的规则条件；
7. 抽取分项裁判结果；
8. 每个节点和桥接边绑定SourceSpan；
9. 标记人工复核状态。

### 6.4 案例检索泄漏控制

用于P4检索的`retrieval_text`只能包含：

```text
标题、案由、当事人主张事实、法院认定事实、证据类型
```

不得包含：

```text
裁判结果、支持/驳回标签、最终金额、总结中直接暴露结果的句子
```

结果字段只允许在召回完成后的案例对比阶段使用。

### 6.5 P3完成标准

- 45件民事案例轻量记录全部通过Schema；
- 非民事数据默认索引数量为0；
- 健身房案例不存在伪造`judgment_result`；
- 深度案例中每个CourtFinding、ReasoningStep、DecisionItem都有SourceSpan；
- 至少一个支持案例和一个限制/边界案例具有真实来源；
- P4可以从统一JSONL读取，不需要解析原始Markdown。

---

## 7. P4：BM25、案例检索、条件投影与追问评分

### 7.1 本周技术选择

一周版本不部署Elasticsearch。采用Python本地BM25实现，并使用中文字符二元组或固定分词器，保证离线可复现。

接口保持可替换：

```text
BM25RuleRetriever → RuleRetriever
BM25CaseRetriever → CaseRetriever
RuleConditionProjector → ConditionProjector
WeightedQuestionPolicy → QuestionPolicy
```

### 7.2 规则索引字段

```text
RuleRecord.title
RuleRecord.claim_types
ProvisionRecord.text
RuleCondition.label
RuleCondition.predicate
LegalConsequence.description
```

### 7.3 案例索引字段

```text
CaseRecord.title
CaseRecord.cause
ClaimRecord.claim_type
CourtFinding.predicate
EvidenceItem.type（字段补充后）
```

禁止索引`DecisionItem.status`和结果描述。

### 7.4 第一版检索流程

```text
用户查询
 → BM25规则Top-5
 → 图/ID扩展规则条件
 → BM25案例Top-20
 → 规则重合与已知条件重排
 → Support/Limit/Boundary集合
```

第一版重排分数：

\[
Score(case)=0.45B+0.25C+0.20R+0.10S
\]

其中：

- `B`：归一化BM25分数；
- `C`：用户已知条件与案例条件重合；
- `R`：候选规则重合；
- `S`：来源和成熟度分数。

本周权重是可解释启发式，不宣称为最优参数。

### 7.5 条件投影

输入：

```text
QueryState + RuleRecord[] + CaseRecord[]
```

输出：

```text
QueryConditionState[]
```

约束：

- 用户未提及 → `UNKNOWN`；
- 明确否定 → `NOT_SATISFIED`；
- 前后回答冲突 → `CONFLICTING`；
- 每个非UNKNOWN状态必须记录`supporting_fact_ids`；
- LLM判断只能作为候选，不能覆盖用户明确回答。

### 7.6 正反案例集合

```text
SupportSet：相同请求获得支持且关键条件相近
LimitSet：事实相近但未支持相同请求
BoundarySet：除1—2个条件外高度接近
```

当真实限制案例不足时返回空集合和`degraded=true`，不得使用占位案例参与正式解释。

### 7.7 第一版追问价值

\[
Utility(c)=0.35R_c+0.30D_c+0.20A_c+0.15E_c-C_c
\]

- \(R_c\)：规则中心性；
- \(D_c\)：正反案例分化；
- \(A_c\)：用户可回答性；
- \(E_c\)：证据可获得性；
- \(C_c\)：交互成本。

如果案例数量不足以计算稳定统计，`D_c`使用离散对照标记：

```text
支持与限制案例状态相反 → 1.0
部分不同                 → 0.5
没有真实对照             → 0.0
```

### 7.8 P4完成标准

- 固定输入下BM25排序可复现；
- 规则检索和案例检索分别有Top-K测试；
- 任何案例结果字段不会进入召回文本；
- 用户缺失事实保持`UNKNOWN`；
- 每个问题能输出评分组成和`why_asked`；
- 没有真实限制案例时系统正确降级；
- 用户回答后不重复询问同一条件。

---

## 8. P5：Streamlit前端、测试集与演示

### 8.1 本周前端选择

使用Streamlit完成演示版，不在一周内建设React工程。原因是现有`legal-rag`已有Streamlit经验，本周重点应放在方法闭环和证据展示。

### 8.2 页面结构

```text
frontend/
├─ app.py
├─ api_client.py
├─ state.py
└─ components/
   ├─ query_input.py
   ├─ claim_panel.py
   ├─ question_card.py
   ├─ condition_matrix.py
   ├─ case_compare.py
   ├─ explanation_panel.py
   └─ citation_drawer.py
```

### 8.3 页面流程

#### 页面A：用户输入

- 大文本框；
- 提交按钮；
- 不提前要求用户填写法律字段。

#### 页面B：系统当前理解

展示：

- 候选请求权；
- 候选规则；
- 已知、未知、冲突条件；
- 当前数据成熟度。

#### 页面C：关键追问

展示：

- 追问；
- 选项和自由补充；
- “为什么询问”；
- 对应规则条件；
- 用户可选择“暂时不知道”或“直接查看当前解释”。

#### 页面D：案例条件矩阵

| 规则条件 | 用户情况 | 支持案例 | 限制案例 |
|---|---|---|---|
| 合同成立 | 满足 | 满足 | 满足 |
| 无法继续履行 | 未知 | 满足 | 不满足 |

不能只显示一个不透明的相似度百分比。

#### 页面E：最终解释

展示：

- 当前主要解释；
- 条件化分支；
- 仍未知的信息；
- 证据准备提示；
- 可展开的法条和案例原文；
- `degraded`、`verified=false`和成熟度警告。

### 8.4 20条固定测试样例

构成：

```text
5条信息相对完整
5条缺少“能否继续履行”
5条缺少“是否存在替代履行”
3条包含前后冲突信息
2条非服务合同问题，用于测试回退
```

每条记录：

```text
initial_query
expected_claim_type
expected_unknown_conditions
expected_first_question
answer
expected_state_after_answer
expected_degradation
```

### 8.5 P5完成标准

- 前端只依赖API和合同示例，不导入后端内部模块；
- 页面刷新后会话ID仍可恢复；
- 用户回答后矩阵同步更新；
- 所有引用可以展开到原文；
- 降级和未验证信息有明显标识；
- 20条样例有逐条执行记录；
- D7主演示可以连续运行3次。

---

## 9. 人员依赖关系

```text
P2 法条/规则 ─────┐
                  ├→ P4 检索/投影/追问 ──┐
P3 案例/跨度 ─────┘                       │
                                          ├→ P1 状态机/API → P5 前端
P1 合同/Repository ───────────────────────┘
```

关键依赖：

1. P2和P3必须共用P1冻结的ID；
2. P4不能等待全部数据，先用examples实现，再切换真实JSONL；
3. P5不能等待正式API，先用`WorkflowSnapshot`固定JSON开发；
4. P1不能等待Neo4j，始终保留内存适配器；
5. 任何人的延误都不能阻断纵向链路的降级运行。

---

## 10. D2—D7排期

| 日期 | P1 | P2 | P3 | P4 | P5 | 集成结果 |
|---|---|---|---|---|---|---|
| D2上午 | v1.1合同、错误模型 | 条文输入审计 | 案例输入审计 | BM25接口和测试语料 | 静态页面 | 12:00合同冻结 |
| D2下午 | 会话Repository、API骨架 | 1,260条转换器 | 45件民事过滤与L1转换器 | BM25规则/案例基线 | 使用固定JSON展示 | 五个模块可独立测试 |
| D3上午 | Neo4j端口和能力接口 | 完成条文导入、校验 | 健身房案例分区和跨度 | 接入P2/P3 JSONL | 接入创建会话API | 数据可被API读取 |
| D3下午 | 回答API | 3—5条规则L3 | 健身房案例L2/L3 | 条件投影矩阵 | 追问卡片 | 无追问链路打通 |
| D4上午 | 状态更新与错误处理 | 修复规则问题 | 补支持/限制案例 | 对比集合与追问评分 | 条件矩阵 | 生成真实关键追问 |
| D4下午 | ExplanationPlan集成 | 法条引用核验 | 案例引用核验 | 停止策略 | 解释与引用面板 | 一轮追问闭环 |
| D5 | 总集成、API测试 | 数据冻结 | 数据冻结 | 算法测试 | 20条样例与界面 | P0功能冻结 |
| D6 | 修阻断缺陷 | 只修数据错误 | 只修来源错误 | 只修算法错误 | 演示彩排 | 不新增功能 |
| D7 | 双机启动、文档 | 支持答疑 | 支持答疑 | 结果记录 | 主演示 | 交付与备份 |

---

## 11. 每日合并制度

### 上午同步，15分钟

每人只回答：

```text
昨天交付了什么可验证文件？
今天将交付什么接口？
现在被谁的哪个字段阻塞？
```

### 12:00第一次合并

- 合同和数据格式；
- 不完整功能允许关闭，但不能破坏main；
- 自动测试必须通过。

### 18:00第二次合并

- 每人提交成功样例和失败样例；
- P1执行全量测试；
- main分支始终保持可运行。

### 合并门槛

```text
ruff check通过
pytest通过
示例JSON通过Schema
不得提交密钥
不得提交未脱敏个人信息
不得提交无来源法律内容
```

---

## 12. 本周验收清单

### 数据

- [ ] 1,260条`ProvisionRecord`；
- [ ] 至少3条L3`RuleRecord`；
- [ ] 45件民事L1案例；
- [ ] 至少8件L2/L3案例；
- [ ] 至少一个真实支持案例和一个真实限制/边界案例；
- [ ] 展示数据全部有SourceSpan。

### 后端

- [ ] 创建会话；
- [ ] 提交回答；
- [ ] 查询会话；
- [ ] BM25规则检索；
- [ ] BM25案例检索；
- [ ] 条件投影；
- [ ] 追问评分；
- [ ] ExplanationPlan；
- [ ] 统一错误和降级状态。

### 前端

- [ ] 用户输入；
- [ ] 当前理解；
- [ ] 关键追问；
- [ ] 条件矩阵；
- [ ] 正反案例；
- [ ] 最终解释；
- [ ] 原文引用；
- [ ] 降级提示。

### 测试

- [ ] 20条固定场景；
- [ ] 不重复追问；
- [ ] 未知事实不被判为否定；
- [ ] 裁判结果不进入案例召回文本；
- [ ] 无真实来源时不会输出正式结论；
- [ ] 新环境按README可以启动；
- [ ] 主演示连续运行3次。

---

## 13. 今天立即开始的任务顺序

1. P1发起v1.1合同变更，补充`LegalSourceRecord`与`ProvisionRecord`；
2. P2用10条法条样本验证转换器，不立即跑1,260条；
3. P3用健身房案例验证章节切分和SourceSpan，不立即批量清洗45件；
4. P4严格根据examples写BM25单元测试；
5. P5根据现有`POST /v1/demo/analyze`返回值做静态界面；
6. 中午一起检查一条规则能否连接一个案例、一个条件和一个原文跨度；
7. 验证成功后再批量处理数据。

该顺序的目的，是先证明最小数据能够贯穿全部接口，再扩大数量。否则五个人可能分别产生大量无法连接的数据和代码。
